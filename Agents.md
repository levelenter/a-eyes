# プロジェクトの概要

このプロジェクトは視覚障害者向けのAIエージェントアプリを作成するプロジェクトです。

# プロジェクトの詳細

1ページのみの構成を持つElectronによるデスクトップアプリケーション。
スクリーンリーダーでよみとりながら利用するモードと、スクリーンリーダーを内包していて独自に読み上げるモードをもつ。

## 起動画面

立ち上げ時にスクリーンリーダーのモードの切り替えをユーザーにといあわせ、そのモードを記憶する。
起動画面で、速度調整も可能とする
Electron版では、作業フォルダの指定も必須とする。

## 作業フォルダ

作業フォルダを指定しそのフォルダ内のファイルを操作することができる。
テキストによるプロンプト入力により、AIエージェントに任意の処理を依頼できる。
ClaudeCodeやClaudeCoworkのように、計画をたて実行をサブエージェントに任せることができる。

ファイル名の指定により、ファイルを開いたり、保存したり、編集したり、削除したりすることができる。
特にPowerPointのファイルを開いて、編集することができる。

## 音声による編集結果の確認

音声による編集結果の確認をスムーズに行える。

## Web版とElectron版の違い

Web版 作業フォルダはサーバーサイドのファイルシステムを利用する。.envファイルに「WEB_WORKING_FOLDER」で記述
Electron版 作業フォルダはローカルのファイルシステムを利用する。

# プロジェクトの技術スタック

- React Nextjs Typescript TailwindCSS Electron
- Claude API,OpenAI API,Gemini API

（現時点ではAPIキーは内部に保持するが、ゆくゆくはサーバーへの認証を介してサーバーからキーを取得してつかう）

## 対応OS

- Windows 64bit
- macOS
